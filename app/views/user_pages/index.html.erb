<div class="container-fluid private-page">

  <%= render :partial => 'navbar/private_nav' %>
  
  <div id="section-1">
    <h1 id="flash-message"><%= flash[:success] %></h1>
    <h1>My Boxes Summary</h1>

    <div class="col-xs-4">
      <h2>@Home</h2>
      <table class="table" data-toggle="table">
        <thead>
          <tr>
            <th>Box ID</th>
            <th>Order ID</th>
          </tr>
          <% @boxes.each do |box| %>
            <% if box.histories.last.location_type_id == 1 %>
              <tr>
                <td><%= box.id %></td>
                <td><%= box.order_id %></td>
              </tr>
            <% end %>
          <% end %>
        </thead>
      </table>
    </div>
    <div class="col-xs-4">
      <h2>@Transit</h2>
      <table class="table" data-toggle="table">
        <thead>
          <tr>
            <th>Box ID</th>
            <th>Order ID</th>
          </tr>
          <% @boxes.each do |box| %>
            <% if box.histories.last.location_type_id == 2 %>
              <tr>
                <td><%= box.id %></td>
                <td><%= box.order_id %></td>
              </tr>
            <% end %>
          <% end %>
        </thead>
      </table>
    </div>
    <div class="col-xs-4">
      <h2>@Warehouse</h2>
      <table class="table" data-toggle="table">
        <thead>
          <tr>
            <th>Box ID</th>
            <th>Order ID</th>
          </tr>
          <% @boxes.each do |box| %>
            <% if box.histories.last.location_type_id == 3 %>
              <tr>
                <td><%= box.id %></td>
                <td><%= box.order_id %></td>
              </tr>
            <% end %>
          <% end %>
        </thead>
      </table>
    </div>
</div>